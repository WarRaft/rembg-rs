#!/bin/bash

# Wrapper script for rembg-rs to set library path

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

export DYLD_LIBRARY_PATH="$SCRIPT_DIR/target/release:$DYLD_LIBRARY_PATH"

exec "$SCRIPT_DIR/target/release/rembg-rs" "$@"
